class Solution{
  public:
    int missingNumber(vector<int>& arr, int n) {
        int sum=0;
        for(int i=0 ; i<n-1 ; i++)
            sum=sum+arr[i];
        int total=n*(n+1)/2;
        return (total-sum);
    }
};
