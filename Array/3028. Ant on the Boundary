class Solution {
    public int returnToBoundaryCount(int[] nums) {
        int n=nums.length;
        int position=0;
        int count=0;
        for(int i=0 ; i<n ; i++){
            position+=nums[i];
            if(position==0){
                count++;
            }
        }
        return count;
    }
}
