class Solution {
    public void reverseString(char[] s) {
        int n=s.length;
        char rev[]=new char[n];
        int idx=0;
        for(int i=n-1 ; i>=0 ; i--){
            rev[idx++]=s[i];
        }
        for(int i=0 ; i<n ; i++){
            s[i]=rev[i];
        }
    }
}
